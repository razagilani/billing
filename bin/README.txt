This directory contains all executable scripts that run in production,
including web applications, upstart tasks, cron jobs, etc.

It does not include database adminstration scripts or upgrade scripts because
those have their own directories.
